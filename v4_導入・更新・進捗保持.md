# PhiloLex // NIGHT CITY v4.0

## デッキ
- 哲学・学術：1500語（150語 × 10セクション）
- 基礎アカデミック：2000語（200語 × 10セクション）
  - 哲学固有語よりも一般的
  - 英検準1級〜1級帯を意識しつつ、論文・評論・高度な英文での有用性を優先
  - 一般上級語と一般学術語を統合

## 学習モード
どのモードでもDECKを先に選べます。
- お任せ
- セクション学習
- 復習モード
- 期限が来た復習
- うろ覚え・未定着
- 新規のみ

「復習モード」は既に一度以上学習した語だけを対象にし、
覚えていない・うろ覚え・期限超過・低定着の語から優先します。

## 新規単語比率
10% / 20% / 30% / ... / 100% を選択できます。
お任せモードとセクション学習に反映します。
100%なら、新規語が残っている限り新規語だけで構成します。

## 進捗保持
通常のGitHub上書き更新では進捗を保持します。
旧v3.xの哲学デッキ進捗はv4で自動的に単語単位の安定したキーへ移行します。

Safariの「Webサイトデータ」を削除するとlocalStorageも消えるため、
削除前に「進捗をバックアップ」でJSONを保存してください。
その後「進捗を復元」で戻せます。

## GitHub更新
1. ZIPを展開
2. GitHubの philolex → Add file → Upload files
3. このフォルダの中身を全部アップロード
4. Commit changes
5. Pages設定は変更不要
6. Safariで BUILD 4.0 を確認


## v4.1 — 基礎2000語の難易度再設計
- 最初の1000語：英検2級上位〜準1級導入を目安
- 次の700語：準1級中心
- 最後の300語：準1級上位〜1級導入
- アルファベット順を廃止
- 一般頻度・抽象度・学術性から推定した難易度が低い順に並べる
- 基礎デッキのカード裏に DIFFICULTY 1/10〜10/10 を表示
- 哲学1500語も一般学術・論証語彙から専門哲学語彙へ概ね難しくなる順へ再配置

### 進捗
進捗は `deck名 + 単語` に紐づいているため、並び順やセクションが変わっても、
v4.0で学習済みの単語がv4.1にも含まれる場合は学習記録を引き継ぎます。

Safari更新確認：BUILD 4.1


## v4.2 — 今日の新規語数
- ホーム画面に「今日の新規」を追加。
- iPhoneのローカル日付（0:00〜23:59）で集計。
- 同じカードをその日に何度復習しても1語として数える。
- そのカードを生涯で初めて学習した日が今日の場合だけ加算。
- デッキをまたいだ合計を表示。
- 学習中HUDにも「今日新規 ○語」を表示。
- 日付が変わると自動的に当日の値へ切り替わる。

### 既存進捗について
v4.2より前にすでに学習済みだった単語には「初回学習日」の記録が存在しないため、
過去分を推測して今日の新規に加えることはしません。
v4.2導入後に初めて学習する単語から正確に集計されます。
既存の学習進捗そのものは保持されます。

Safari更新確認：BUILD 4.2


## v4.3 — 第三デッキ「英文読解フレーズ1500」
第三デッキを正式に追加しました。アプリ全体では合計5000項目です。

### 第三デッキの構成
150項目 × 10 SECTION：

1. BASIC CHUNKS — 超基礎
2. VERB CHUNKS — 基礎動詞句
3. GRAMMAR FRAMES — 基礎構文
4. LOGIC LINKS — 因果・対比・条件・譲歩
5. REFERENCE — 指示・数量・抽象名詞句
6. ACADEMIC CORE — 一般学術表現
7. SOURCES & EVIDENCE — 先行研究・証拠
8. ARGUMENT FRAMES — 論証・反論・応答
9. HEDGING & SCOPE — 留保・限定
10. PHILOSOPHY FRAMES — 哲学論文

各カードは英語表現、日本語の意味、例文、例文日本語訳、Reading noteを持ちます。

### 学習モード
DECK欄で「英文読解フレーズ1500」を選べます。
お任せ / セクション / 復習 / DUE / うろ覚え・未定着 / 新規のみ、の全モードに対応します。

### 進捗
進捗キーは `deck名 + 表現` なので、既存2デッキの進捗は変更しません。
「今日の新規」は第三デッキも含めた3デッキ横断合計です。

Safari更新確認：BUILD 4.3


## v4.4 — AUTO NETをSection順進行へ変更
お任せモード（AUTO NET）の新規カード選定方式を変更しました。

### 新規カード
- 選択したデッキの **最も低い未完了Section** から出題。
- Section内ではデッキ順（易→難）で進行。
- そのセッション中にSectionをすべて消化した場合だけ、次のSectionへ進む。
- したがって Section 1 → 2 → 3 → ... の順を崩しません。

### 復習カード
- すでに学習したカードは **Sectionをまたいで全範囲から選択**。
- 「覚えていない」「うろ覚え」「期限超過」「定着度が低い」の順に弱点を優先。
- 例：Section 4を新規学習中でも、Section 1の弱点カードは引き続き自動復習されます。

### NEWのみ
「新規のみ」モードもSection 1 → 2 → 3 ... の順に変更しました。

### SECTIONモード
手動の「セクション学習」は従来どおり、指定したSectionだけを対象にします。

### 表示
ホーム画面と学習HUDに「AUTO新規：現在のSection / 未学習数」を表示します。

既存進捗は `deck名 + 単語/表現` で保存されているため、そのまま保持されます。

Safari更新確認：BUILD 4.4
